import React from "react";
import styles from './Title.module.css';

const Title = () => {
    return (
        <div>
            <h1 className={styles.title}>Take me home!</h1>
        </div>
    );
}

export default Title